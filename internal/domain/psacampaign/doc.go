// Package psacampaign models PSA Buyer Campaign Manager offer-program config
// (which subjects/sets PSA solicits, buy terms, buy-box bounds) and the
// approval queue for pushing edits back to the portal. Money is in cents.
package psacampaign
