[
  {
    "id": "ARCH-001",
    "lens": "architecture",
    "revision": "740976ecf80a4f2ccdaa611d7790ccaa95b48773",
    "subject": {
      "kind": "symbol",
      "identity": "internal/domain/inventory.DailySpend (CapCents, FillRatePct) + fill-rate rule in internal/adapters/httpserver/handlers/campaigns_analytics.go"
    },
    "category": "architecture",
    "title": "Fill-rate business rule lives in an HTTP handler; the domain fields designed to carry it are never populated by the only repository",
    "severity": "medium",
    "confidence": "strong",
    "evidence": [
      {
        "claim": "The domain type DailySpend declares CapCents and FillRatePct, with a doc comment defining the rule as 'spend / cap'.",
        "command": "sed -n '28,36p' internal/domain/inventory/analytics_types.go",
        "output": "// DailySpend holds actual spend vs cap for a given date.\ntype DailySpend struct {\n\tDate          string  `json:\"date\"`\n\tSpendCents    int     `json:\"spendCents\"`\n\tCapCents      int     `json:\"capCents\"`\n\tFillRatePct   float64 `json:\"fillRatePct\"` // spend / cap\n\tPurchaseCount int     `json:\"purchaseCount\"`\n}",
        "file_line": "internal/domain/inventory/analytics_types.go:29-35"
      },
      {
        "claim": "The sole non-test implementation of AnalyticsRepository.GetDailySpend selects only three columns and scans only Date, SpendCents, PurchaseCount. CapCents and FillRatePct are therefore always zero on every value that crosses the repository boundary.",
        "command": "sed -n '81,100p' internal/adapters/storage/postgres/analytics_store.go",
        "output": "func (as *AnalyticsStore) GetDailySpend(ctx context.Context, campaignID string, days int) ([]inventory.DailySpend, error) {\n\tquery := `\n\t\tSELECT purchase_date,\n\t\t\tSUM(buy_cost_cents + psa_sourcing_fee_cents) AS spend,\n\t\t\tCOUNT(*) AS purchase_count\n\t\tFROM campaign_purchases\n\t\t...\n\treturn scanRows(ctx, rows, func(rs *sql.Rows) (inventory.DailySpend, error) {\n\t\tvar ds inventory.DailySpend\n\t\terr := rs.Scan(&ds.Date, &ds.SpendCents, &ds.PurchaseCount)\n\t\treturn ds, err\n\t})",
        "file_line": "internal/adapters/storage/postgres/analytics_store.go:81-99"
      },
      {
        "claim": "The HTTP handler discards the domain field and recomputes fill rate itself, substituting the campaign's CURRENT DailySpendCapCents for every historical day.",
        "command": "sed -n '83,98p' internal/adapters/httpserver/handlers/campaigns_analytics.go",
        "output": "\trows := make([]fillRateRow, len(daily))\n\tfor i, d := range daily {\n\t\tcapCents := d.CapCents\n\t\tif capErr == nil && campaign != nil {\n\t\t\tcapCents = campaign.DailySpendCapCents\n\t\t}\n\t\tfillRate := d.FillRatePct\n\t\tif capCents > 0 {\n\t\t\tfillRate = float64(d.SpendCents) / float64(capCents)\n\t\t}",
        "file_line": "internal/adapters/httpserver/handlers/campaigns_analytics.go:86-93"
      },
      {
        "claim": "The same rule is implemented a second time, independently, inside the domain tuning rule \u2014 also ignoring ds.CapCents and using the campaign's current cap.",
        "command": "sed -n '232,240p' internal/domain/inventory/tuning.go",
        "output": "\tvar totalFill float64\n\tfor _, ds := range recent {\n\t\tif campaign.DailySpendCapCents > 0 {\n\t\t\ttotalFill += float64(ds.SpendCents) / float64(campaign.DailySpendCapCents)\n\t\t}\n\t}\n\tavgFill := totalFill / float64(len(recent))",
        "file_line": "internal/domain/inventory/tuning.go:232-238"
      },
      {
        "claim": "GetDailySpend has exactly two non-test consumers: the HTTP handler and the tuning service. There is no third implementation of the repository method that might populate the missing fields.",
        "command": "git grep -nE 'GetDailySpend' -- 'internal/**/*.go' 'cmd/**/*.go' | grep -v _test | grep -v testutil",
        "output": "internal/adapters/httpserver/handlers/campaigns_analytics.go:57:\tdaily, err := h.service.GetDailySpend(r.Context(), id, days)\ninternal/adapters/storage/postgres/analytics_store.go:81:func (as *AnalyticsStore) GetDailySpend(ctx context.Context, campaignID string, days int) ([]inventory.DailySpend, error) {\ninternal/domain/inventory/repository_analytics.go:9:\tGetDailySpend(ctx context.Context, campaignID string, days int) ([]DailySpend, error)\ninternal/domain/inventory/service_analytics.go:25:func (s *service) GetDailySpend(ctx context.Context, campaignID string, days int) ([]DailySpend, error) {\ninternal/domain/inventory/service_analytics.go:26:\treturn s.analytics.GetDailySpend(ctx, campaignID, days)\ninternal/domain/inventory/service_interfaces.go:31:\tGetDailySpend(ctx context.Context, campaignID string, days int) ([]DailySpend, error)\ninternal/domain/tuning/service.go:77:\t\tdailySpend, err = s.analytics.GetDailySpend(gCtx, campaignID, 30)"
      }
    ],
    "searched_universe": "git ls-files 'internal/**/*.go' 'cmd/**/*.go' at revision 740976ec, default build tags.",
    "exclusions": [
      "_test.go files excluded from consumer counts (test usage is noted separately and does not populate production fields)",
      "web/src (frontend consumption of the fillRatePct JSON field is out of this lens)"
    ],
    "build_tags": [
      "default"
    ],
    "proposed_fix": "Move the fill-rate computation into internal/domain/inventory as a single exported function over (DailySpend, Campaign) and call it from both internal/domain/tuning and the handler, or have the service populate DailySpend.CapCents/FillRatePct before returning. Then delete the handler-local arithmetic. If the historical per-day cap is genuinely unavailable, delete DailySpend.CapCents and its 'spend / cap' comment so the type stops advertising a field nothing fills.",
    "blast_radius": [
      "internal/domain/inventory/analytics_types.go",
      "internal/domain/inventory/tuning.go",
      "internal/domain/inventory/service_analytics.go",
      "internal/adapters/httpserver/handlers/campaigns_analytics.go",
      "internal/adapters/storage/postgres/analytics_store.go",
      "web/src (the fillRatePct JSON field value is unchanged only if the same cap is used)"
    ],
    "effort": "M",
    "acceptance_criteria": [
      "Exactly one expression computing spend/cap remains in the repository: `git grep -nE 'SpendCents\\) / float64' -- internal/ | grep -v _test` returns a single line, inside internal/domain/.",
      "A domain-level table-driven unit test covers the fill-rate rule (including capCents == 0) without constructing an http.Request.",
      "GET /api/campaigns/{id}/fill-rate returns the same fillRatePct values as before the change for a campaign whose daily cap has never been edited.",
      "`go build ./...`, `go test -race ./...`, and `make check` pass."
    ]
  },
  {
    "id": "ARCH-002",
    "lens": "architecture",
    "revision": "740976ecf80a4f2ccdaa611d7790ccaa95b48773",
    "subject": {
      "kind": "symbol",
      "identity": "internal/domain/demand.Repository \u2014 CardCache/CharacterCache JSON blob fields (DemandJSON, VelocityJSON, TrendJSON, SaturationJSON, PriceDistributionJSON)"
    },
    "category": "architecture",
    "title": "The demand repository contract passes opaque vendor JSON blobs through the domain; writer and reader share no Go type, so the compiler enforces nothing across the seam",
    "severity": "medium",
    "confidence": "strong",
    "evidence": [
      {
        "claim": "The domain repository DTOs carry five raw-JSON *string fields that are verbatim storage column contents.",
        "command": "sed -n '13,28p' internal/domain/demand/types.go",
        "output": "// CardCache is the domain view of a dh_card_cache row. ...\ntype CardCache struct {\n\tCardID                string\n\t...\n\tDemandJSON            *string\n\tVelocityJSON          *string\n\tTrendJSON             *string\n\tSaturationJSON        *string\n\tPriceDistributionJSON *string",
        "file_line": "internal/domain/demand/types.go:15-27"
      },
      {
        "claim": "The blob is produced in an adapter (the scheduler) by marshalling a DH client entry type.",
        "command": "sed -n '139,146p' internal/adapters/scheduler/dh_analytics_refresh_steps.go",
        "output": "\t\tif entry, ok := velocityByChar[name]; ok {\n\t\t\tif blob, encErr := json.Marshal(entry.Velocity); encErr == nil {\n\t\t\t\tstr := string(blob)\n\t\t\t\trow.VelocityJSON = &str",
        "file_line": "internal/adapters/scheduler/dh_analytics_refresh_steps.go:140-143"
      },
      {
        "claim": "The blob is consumed in the domain by unmarshalling into a hand-mirrored, unexported struct that duplicates the field names as string tags. Nothing ties the two structs together at compile time.",
        "command": "sed -n '279,297p' internal/domain/demand/service.go",
        "output": "// velocityBlobJSON / characterSaturationJSON mirror the cached JSON\n// blobs for a character row's velocity + saturation surfaces.\n...\ntype velocityBlobJSON struct {\n\tMedianDaysToSell   *float64 `json:\"median_days_to_sell\"`\n\tSampleSize         int      `json:\"sample_size\"`\n\tVelocityChangePct  *float64 `json:\"velocity_change_pct\"`",
        "file_line": "internal/domain/demand/service.go:279-297"
      },
      {
        "claim": "A storage-decoding failure mode has leaked all the way into the API response type: the domain counts unparseable blobs and returns the count as a response field.",
        "command": "sed -n '137,156p' internal/domain/demand/campaign_signals.go",
        "output": "func buildSignalIndex(rows []CharacterCache) (map[string]signalEntry, int) {\n\t...\n\t\tvar v velocityBlobJSON\n\t\tif err := json.Unmarshal([]byte(*row.VelocityJSON), &v); err != nil {\n\t\t\tskipped++ // non-nil velocity_json failed to parse \u2014 unexpected",
        "file_line": "internal/domain/demand/campaign_signals.go:137-152"
      },
      {
        "claim": "The package doc states the intended decoupling that this seam does not achieve \u2014 the wire format is exactly what crosses the repository interface.",
        "command": "sed -n '1,7p' internal/domain/demand/types.go",
        "output": "// Package demand ... DH JSON\n// payloads are parsed here into domain-local structs so the scoring logic\n// stays decoupled from the wire format.",
        "file_line": "internal/domain/demand/types.go:1-7"
      }
    ],
    "searched_universe": "git ls-files 'internal/domain/demand/*.go' 'internal/adapters/scheduler/*.go' 'internal/adapters/storage/postgres/dh_demand_repository.go' at revision 740976ec, default build tags.",
    "exclusions": [
      "_test.go files",
      "The db-map lens owns the dh_card_cache / dh_character_cache column definitions themselves"
    ],
    "build_tags": [
      "default"
    ],
    "proposed_fix": "Define the velocity / demand / saturation payloads as exported domain structs in internal/domain/demand and change Repository to accept and return those structs. Let the postgres adapter own the JSON marshal/unmarshal (it already owns nullStringFromPtr/nullStringToPtr), and let the scheduler build the domain struct instead of a JSON string. A renamed DH field then breaks the build in the adapter rather than silently nilling a pointer in the leaderboard.",
    "blast_radius": [
      "internal/domain/demand/types.go",
      "internal/domain/demand/repository.go",
      "internal/domain/demand/service.go",
      "internal/domain/demand/campaign_signals.go",
      "internal/adapters/storage/postgres/dh_demand_repository.go",
      "internal/adapters/scheduler/dh_analytics_refresh_steps.go",
      "internal/testutil/mocks (demand repository mock)"
    ],
    "effort": "L",
    "acceptance_criteria": [
      "`git grep -nE 'json.Unmarshal' -- internal/domain/demand/ | grep -v _test` returns empty.",
      "internal/domain/demand contains no field whose name ends in `JSON` holding a serialized payload.",
      "A unit test asserts that a DH velocity payload written by the scheduler round-trips to the domain struct the leaderboard reads, using the same Go type on both sides.",
      "GET the niches leaderboard and campaign-signals endpoints return byte-identical JSON for a fixture dataset before and after the change.",
      "`go build ./...`, `go test -race ./...`, and `make check` pass."
    ]
  },
  {
    "id": "ARCH-003",
    "lens": "architecture",
    "revision": "740976ecf80a4f2ccdaa611d7790ccaa95b48773",
    "subject": {
      "kind": "symbol",
      "identity": "tombstoneThreshold (declared independently in internal/adapters/clients/dhprice and internal/adapters/storage/postgres)"
    },
    "category": "architecture",
    "title": "The DH tombstone policy has no home in the domain: the threshold is duplicated as an unexported constant in two adapter packages that are forbidden from importing each other",
    "severity": "low",
    "confidence": "strong",
    "evidence": [
      {
        "claim": "The same policy constant is declared twice, in two separate adapter packages, and used to make the same decision in each.",
        "command": "git grep -n 'tombstoneThreshold' -- 'internal/**/*.go' | grep -v _test",
        "output": "internal/adapters/clients/dhprice/provider.go:21:\ttombstoneThreshold = 3\ninternal/adapters/clients/dhprice/provider.go:171:\tif n >= tombstoneThreshold {\ninternal/adapters/storage/postgres/dh_card_tombstone_store.go:10:const tombstoneThreshold = 3\ninternal/adapters/storage/postgres/dh_card_tombstone_store.go:32:\treturn attempts >= tombstoneThreshold, nil\ninternal/adapters/storage/postgres/dh_card_tombstone_store.go:77:\t\ttombstoneThreshold,"
      },
      {
        "claim": "The domain interface encodes the rule only as English in a doc comment \u2014 there is no domain-level constant or predicate the two adapters could share.",
        "command": "sed -n '206,228p' internal/domain/pricing/provider.go",
        "output": "type DHCardTombstoneRepo interface {\n\t// IsTombstoned returns true if the card has reached the tombstone threshold.\n\tIsTombstoned(ctx context.Context, cardID int) (bool, error)\n\n\t// RecordFailure increments the failure counter for cardID and returns the\n\t// new attempt count. Callers should treat attempts >= 3 as the tombstone\n\t// threshold.\n\t...\n\t// Count returns the number of cards currently tombstoned (attempts >= 3).",
        "file_line": "internal/domain/pricing/provider.go:206-228"
      },
      {
        "claim": "The two declaring packages cannot share a constant directly: the architecture check forbids storage adapters from importing client adapters, so the only legal shared home is internal/domain or internal/platform, and neither holds one.",
        "command": "sed -n '23,34p' scripts/check-imports.sh",
        "output": "violations_storage=$(grep -rn '\"github.com/guarzo/slabledger/internal/adapters/clients' internal/adapters/storage/ 2>/dev/null || true)\n\nif [ -n \"$violations_storage\" ]; then\n  echo \"ERROR: Storage adapters must not import client adapters.\"",
        "file_line": "scripts/check-imports.sh:23-34"
      },
      {
        "claim": "No shareable tombstone threshold identifier exists anywhere under internal/domain or internal/platform \u2014 the rule appears there only inside comments (provider.go:212, :226), never as code.",
        "command": "git grep -nE '^[^/]*[Tt]ombstoneThreshold' -- 'internal/domain/**/*.go' 'internal/platform/**/*.go'",
        "output": "(no output; exit status 1)"
      }
    ],
    "searched_universe": "git ls-files 'internal/**/*.go' at revision 740976ec, default build tags.",
    "exclusions": [
      "_test.go files",
      "internal/testutil/mocks/dh_card_tombstone_repo.go (a mock; it does not implement the threshold rule)"
    ],
    "build_tags": [
      "default"
    ],
    "proposed_fix": "Add an exported constant (or an IsTombstoned(attempts int) bool predicate) to internal/domain/pricing next to DHCardTombstoneRepo, and have both adapters reference it. Replace the 'attempts >= 3' prose in the interface doc with a pointer to the constant.",
    "blast_radius": [
      "internal/domain/pricing/provider.go",
      "internal/adapters/clients/dhprice/provider.go",
      "internal/adapters/storage/postgres/dh_card_tombstone_store.go"
    ],
    "effort": "S",
    "acceptance_criteria": [
      "`git grep -n '= 3' -- internal/adapters/clients/dhprice internal/adapters/storage/postgres/dh_card_tombstone_store.go | grep -i tombstone` returns empty; the only declaration is under internal/domain/.",
      "A domain unit test pins the threshold boundary (attempts 2 -> not tombstoned, 3 -> tombstoned).",
      "The existing internal/adapters/storage/postgres/dh_card_tombstone_store_test.go passes unchanged.",
      "`go build ./...`, `go test -race ./...`, and `make check` pass."
    ]
  },
  {
    "id": "ARCH-004",
    "lens": "architecture",
    "revision": "740976ecf80a4f2ccdaa611d7790ccaa95b48773",
    "subject": {
      "kind": "type",
      "identity": "internal/domain/inventory.Service and its seven sub-interfaces in service_interfaces.go"
    },
    "category": "architecture",
    "title": "The inventory Service interface-segregation split buys no seam: four of seven sub-interfaces have zero consumers, consumers that want narrow deps declare their own, and the main handler still takes the 54-method union",
    "severity": "low",
    "confidence": "strong",
    "evidence": [
      {
        "claim": "The Service union documents the segregation as its purpose.",
        "command": "sed -n '170,185p' internal/domain/inventory/service.go",
        "output": "// Service is the full campaigns API \u2014 a composition of all sub-interfaces.\n// Consumers that only need a subset should depend on the narrower interface\n// (e.g. AnalyticsService, ImportService) to follow the Interface Segregation Principle.\ntype Service interface {\n\tCRUDService\n\tAnalyticsService\n\tImportService\n\tPricingService\n\tCertLookupService\n\tSnapshotService\n\tDHService",
        "file_line": "internal/domain/inventory/service.go:170-186"
      },
      {
        "claim": "The Phase 1 Go reference map records four of the seven sub-interfaces with zero external references, none flagged name_ambiguous.",
        "command": "jq -r '.records[] | select(.identity|test(\"inventory.(CRUDService|DHService|CertLookupService|SnapshotService)$\")) | \"\\(.identity) refs=\\(.external_refs) amb=\\(.name_ambiguous)\"' docs/audit/maps/go-reference-map.json",
        "output": "github.com/guarzo/slabledger/internal/domain/inventory.CRUDService refs=0 amb=null\ngithub.com/guarzo/slabledger/internal/domain/inventory.DHService refs=0 amb=null\ngithub.com/guarzo/slabledger/internal/domain/inventory.CertLookupService refs=0 amb=null\ngithub.com/guarzo/slabledger/internal/domain/inventory.SnapshotService refs=0 amb=null"
      },
      {
        "claim": "Only three of the seven are used by real consumers, in three places total.",
        "command": "git grep -nE '\\binventory\\.(AnalyticsService|ImportService|PricingService|CRUDService|DHService|CertLookupService|SnapshotService)\\b' -- 'internal/**/*.go' 'cmd/**/*.go' | grep -v _test | grep -v testutil",
        "output": "internal/adapters/httpserver/handlers/price_flags_handler.go:13:\tservice inventory.PricingService\ninternal/adapters/httpserver/handlers/price_flags_handler.go:18:func NewPriceFlagsHandler(service inventory.PricingService, logger observability.Logger) *PriceFlagsHandler {\ninternal/adapters/scheduler/dh_orders_poll.go:48:\tcampaignSvc inventory.ImportService\ninternal/adapters/scheduler/dh_orders_poll.go:58:\tcampaignSvc inventory.ImportService,\ninternal/adapters/scoring/provider.go:17:\tsvc       inventory.AnalyticsService\ninternal/adapters/scoring/provider.go:30:func NewProvider(svc inventory.AnalyticsService, opts ...ProviderOption) *Provider {"
      },
      {
        "claim": "Adapters that wanted a narrow dependency bypassed the published sub-interfaces and declared their own local subsets instead.",
        "command": "git grep -nE 'subset of inventory\\.' -- internal/adapters | grep -v _test",
        "output": "internal/adapters/httpserver/handlers/psa_sync_handler.go:26:// PSASyncPurchaseCreator creates purchases (subset of inventory.Service).\ninternal/adapters/scheduler/snapshot_enrich.go:11:// SnapshotEnrichService is the subset of inventory.Service needed by the enrichment scheduler."
      },
      {
        "claim": "The concrete cost: consumers that take the union force a 546-line mock covering 54 methods.",
        "command": "wc -l internal/testutil/mocks/inventory_service.go && grep -cE '^\\t[A-Z][A-Za-z0-9]*\\(' internal/domain/inventory/service_interfaces.go && git grep -nE 'inventory\\.Service\\b' -- internal/adapters | grep -v _test",
        "output": "546 internal/testutil/mocks/inventory_service.go\n54\ninternal/adapters/advisortool/executor.go:23:// CampaignToolExecutor implements ai.ToolExecutor by calling inventory.Service methods.\ninternal/adapters/advisortool/executor.go:25:\tsvc            inventory.Service\ninternal/adapters/advisortool/executor.go:82:func NewCampaignToolExecutor(svc inventory.Service, opts ...ExecutorOption) *CampaignToolExecutor {\ninternal/adapters/httpserver/handlers/campaigns.go:32:\tservice        inventory.Service\ninternal/adapters/httpserver/handlers/campaigns.go:98:\tservice inventory.Service,\ninternal/adapters/httpserver/handlers/psa_sync_handler.go:26:// PSASyncPurchaseCreator creates purchases (subset of inventory.Service).\ninternal/adapters/httpserver/router.go:65:\tCampaignsService          inventory.Service\ninternal/adapters/scheduler/snapshot_enrich.go:11:// SnapshotEnrichService is the subset of inventory.Service needed by the enrichment scheduler.\ninternal/adapters/scoring/provider.go:1:// Package scoring implements advisor.ScoringDataProvider by wrapping inventory.Service."
      }
    ],
    "searched_universe": "git ls-files 'internal/**/*.go' 'cmd/**/*.go' at revision 740976ec, plus docs/audit/maps/go-reference-map.json for reachability. Default build tags.",
    "exclusions": [
      "_test.go files and internal/testutil/mocks excluded from consumer counts",
      "The zero-reference status of the four sub-interfaces as a dead-code claim is owned by the dead-code lens; here it is evidence that the seam is unused, not a deletion request"
    ],
    "build_tags": [
      "default"
    ],
    "proposed_fix": "Either (a) split CampaignsHandler and CampaignToolExecutor to depend on the narrow interfaces they actually use and fold the two ad-hoc local subsets (PSASyncPurchaseCreator, SnapshotEnrichService) into the published set, or (b) accept that the union is the real contract, delete the four zero-consumer sub-interfaces, inline their methods into Service, and remove the ISP claim from the doc comment. Do not leave the split declared but unused.",
    "blast_radius": [
      "internal/domain/inventory/service.go",
      "internal/domain/inventory/service_interfaces.go",
      "internal/adapters/httpserver/handlers/campaigns.go",
      "internal/adapters/httpserver/router.go",
      "internal/adapters/advisortool/executor.go",
      "internal/testutil/mocks/inventory_service.go"
    ],
    "effort": "M",
    "acceptance_criteria": [
      "Every interface declared in internal/domain/inventory/service_interfaces.go has at least one non-test consumer, verifiable with a single git grep per name.",
      "No adapter declares a comment matching 'subset of inventory.Service' \u2014 narrow needs are met by a published interface.",
      "`go build ./...`, `go test -race ./...`, and `make check` pass.",
      "No change to any HTTP response shape: the handler test suite passes unmodified."
    ]
  },
  {
    "id": "ARCH-005",
    "lens": "architecture",
    "revision": "740976ecf80a4f2ccdaa611d7790ccaa95b48773",
    "subject": {
      "kind": "symbol",
      "identity": "internal/domain/errors.(*AppError).WithHTTPStatus"
    },
    "category": "dead-code",
    "title": "POINTER (dead-code lens): AppError.WithHTTPStatus appears to have no non-test caller, and its sibling HTTPStatus() has exactly one, in a retry policy",
    "severity": "low",
    "confidence": "suspected",
    "evidence": [
      {
        "claim": "Outside its own declaration and doc comments, WithHTTPStatus has no call site; HTTPStatus() has one, in internal/platform/resilience.",
        "command": "git grep -nE 'HTTPStatus\\(\\)|WithHTTPStatus' -- 'internal/**/*.go' 'cmd/**/*.go' | grep -v _test",
        "output": "internal/domain/auth/errors.go:16:// mutate (e.g. WithContext, WithHTTPStatus) without affecting the sentinel.\ninternal/domain/errors/errors.go:94:func (e *AppError) HTTPStatus() int {\ninternal/domain/errors/errors.go:145:// safe to mutate (WithContext, WithHTTPStatus) without affecting the original.\ninternal/domain/errors/errors.go:162:// WithHTTPStatus sets the explicit HTTP status code for the error.\ninternal/domain/errors/errors.go:165:func (e *AppError) WithHTTPStatus(statusCode int) *AppError {\ninternal/platform/resilience/retry.go:142:\t\tif httpStatus := appErr.HTTPStatus(); httpStatus > 0 {"
      },
      {
        "claim": "The Phase 1 map records one external reference for the method, consistent with test-only usage.",
        "command": "jq -r '.records[] | select(.identity|test(\"WithHTTPStatus\")) | \"\\(.identity) refs=\\(.external_refs) amb=\\(.name_ambiguous)\"' docs/audit/maps/go-reference-map.json",
        "output": "github.com/guarzo/slabledger/internal/domain/errors.(e *AppError).WithHTTPStatus refs=1 amb=null\ngithub.com/guarzo/slabledger/internal/domain/errors.TestAppError_WithHTTPStatus refs=0 amb=null\ngithub.com/guarzo/slabledger/internal/domain/errors.TestAppError_WithHTTPStatus_OverwritePreviousValue refs=0 amb=null\ngithub.com/guarzo/slabledger/internal/domain/errors.TestAppError_WithHTTPStatus_Validation refs=0 amb=null"
      }
    ],
    "searched_universe": "git ls-files 'internal/**/*.go' 'cmd/**/*.go' at revision 740976ec, default build tags, plus docs/audit/maps/go-reference-map.json.",
    "exclusions": [
      "Not investigated to dead-code standard: the nine runtime-reachability checks were not run. This is a pointer for the dead-code lens, not a deletion recommendation."
    ],
    "build_tags": [
      "default"
    ],
    "proposed_fix": "Dead-code lens to confirm reachability. If the setter is genuinely unreachable, note that the sole reader (resilience/retry.go:142) uses the field to hold an UPSTREAM provider's response status for retry decisions, not a status to emit \u2014 so the surviving concept may want renaming rather than deletion. That naming question belongs to the naming-and-boundaries lens.",
    "blast_radius": [
      "internal/domain/errors/errors.go",
      "internal/platform/resilience/retry.go"
    ],
    "effort": "S",
    "acceptance_criteria": [
      "Dead-code lens returns a verdict on WithHTTPStatus with all nine runtime checks answered.",
      "If removed: `go build ./...` and `go test ./...` pass with the method and its three tests deleted."
    ]
  }
]
