[
  {
    "id": "DUP-001",
    "verdict": "confirmed_lower_severity",
    "evidence_reproduces": false,
    "basis": "I re-derived the duplication from source rather than from the finding. Reading internal/domain/dhlisting/dh_push_safety.go:26-50 and internal/domain/dhpricing/service.go:47-70 at 06d9f8ce, the two resolvers are equivalent in behavior, not merely similar in text: identical guard order (override==0 -> reviewed; reviewed==0 -> override; overrideNewer -> override; else reviewed), identical parameter type (*inventory.Purchase), identical return type (bare int), and both overrideNewer helpers parse RFC3339 on both operands, compare with .After on double-success, and fall back to raw string comparison otherwise. There is no parameterization to differ on, so the 'deliberately-differing sibling' false positive does not apply here. The finding's md5 pair reproduces byte-for-byte (68f4ba7f3bcdbdeed3463abde65c7208 twice). The absence claim on shared source holds by construction: `git grep -nE '\\boverrideNewer\\b' 06d9f8ce` returns two independent func definitions, one per package, and no third home; `git ls-tree -r --name-only 06d9f8ce -- internal/domain/dhpricing internal/domain/dhlisting` shows no shared file. No test asserts the two paths agree. Both copies are live: the dhlisting original is called in-package at dh_push_safety.go:60 and :141 plus six external sites, and the dhpricing copy gates SyncPurchasePrice at service.go:87.",
    "what_the_finding_got_wrong": "Evidence item 4 is false, and it is the leg carrying the severity. The finding states 'Only the dhlisting copy is covered by a test. There is no test in dhpricing that exercises resolveListingPrice's reviewed/override precedence.' internal/domain/dhpricing/service_test.go:225, TestSyncPurchasePrice_ResolvesNewestCommit, is a two-case table that sets ReviewedPriceCents/ReviewedAt and OverridePriceCents/OverrideSetAt on the same purchase and asserts the cents value handed to the updater — and its second case is exactly the cross-offset instant-vs-lexicographic case ('2026-04-21T08:00:00-05:00' against '2026-04-21T12:00:00Z') that both source comments name as the subtle failure mode. The lens missed it because its coverage command greps for the function name, and this test reaches the resolver through the exported SyncPurchasePrice instead of naming it — the in-package-caller trap of LENS-BRIEF §3 running in the direction that makes covered code look uncovered. So the claimed consequence (divergence in the hard case goes undetected on the dhpricing side) does not follow: a one-sided edit to dhpricing.overrideNewer that broke parsed comparison would fail dhpricing's own suite. What remains true and unaddressed is narrower — dhpricing has no cases for the zero-value guards or the empty-timestamp lexicographic fallback that dhlisting's nine-case table covers, and nothing asserts the two agree. Separately, the finding presents the duplication as if it were unexamined; it is a recorded decision, explicitly scoped out at docs/specs/2026-08-08-flat-sibling-checker-design.md:189 and docs/plans/2026-08-08-flat-sibling-checker.md:589, which the fixer should read before re-litigating it. The proposed fix and acceptance criteria are otherwise sound and the hub is a legal shared home per CLAUDE.md:126.",
    "corrected_severity": "medium",
    "ticketable": true
  },
  {
    "id": "DUP-002",
    "verdict": "confirmed",
    "evidence_reproduces": true,
    "basis": "Re-derived by reading internal/adapters/storage/postgres/purchase_scan_helpers.go:54-66 and 74-190 in full. saleColumnsAliased lists 23 columns; saleColumns lists 29; the six absent from the aliased list are exactly original_list_price_cents, price_reductions, days_listed, sold_at_asking_price, was_cracked, order_id. The counting command reproduces (23 / 29). Each list pairs with its own destination sequence — scanSale at :74 for saleColumns, and a hand-rolled 23-local sql.Null* block at :146-190 for the aliased one — so both are internally arity-consistent and Scan cannot report a mismatch; the divergence is silent by construction. The write path does populate the six (sale_store.go:38-39). I ran my own repo-wide check rather than the finding's narrowed one: `git grep -nE '\\b(OriginalListPriceCents|PriceReductions|DaysListed|SoldAtAskingPrice|WasCracked|OrderID)\\b' 06d9f8ce -- '*.go' | grep -v _test.go` yields only the two postgres scan/insert sites, the struct declarations, the csvimport/DH-order write paths, and inventory/service_crud.go:308-310 — no read of any of the six off a PurchaseWithSale. I also traced every PurchaseWithSale consumer (arbitrage, portfolio, tuning, inventory/portfolio.go) and none reaches an HTTP handler, so the zeroed fields are not on the wire either. That matches the finding's own 'latent, not currently firing' characterization, which is why medium rather than high is the right filing. The purchase half already has the fix shape (purchaseScanDests at :107, shared by both scan functions), so the asymmetry is real and the proposed fix is the local idiom.",
    "what_the_finding_got_wrong": "Only a framing nit: the title asserts a JOIN-loaded Sale 'silently returns zeroed sale-outcome fields' without the qualifier, while evidence item 5 correctly establishes no consumer reads them. A fixer reading the title alone would expect a live defect. The body is accurate and the severity is honestly calibrated.",
    "corrected_severity": "",
    "ticketable": true
  },
  {
    "id": "DUP-003",
    "verdict": "confirmed_lower_severity",
    "evidence_reproduces": true,
    "basis": "I settled reachability by my own word-anchored, revision-pinned search rather than by the map, which cannot resolve it (auth.New records external_refs=280 with name_ambiguous=true across 5 distinct `New` definitions). `git grep -nE '\\bauth\\.New\\b' 06d9f8ce -- '*.go'` yields service_impl.go:46 (an error string), service_test.go:56 and :76 (the only two calls), and a doc comment in testutil/mocks/auth_inmemory.go:19. I closed the alias hole the bare pattern would miss: cmd/slabledger/server.go imports the package as domainAuth, and `git grep -nE '\\bdomainAuth\\.New\\b'` returns nothing; the only production construction is google.NewOAuthService at cmd/slabledger/main.go:232. Nothing inside package auth calls New either — the sole `func New(` is its own definition at service_impl.go:44. Both drift claims hold at source: OAuthService.ValidateSession performs the 60-second-throttled repo.UpdateSessionAccess (oauth.go:240-247) and authService.ValidateSession returns immediately after the user lookup (service_impl.go:109-123); the 30-day expiry window is declared twice, service_impl.go:13 and oauth.go:46, with no shared source.",
    "what_the_finding_got_wrong": "'Two full implementations' overstates what authService is, and that overstatement drives the severity. Its own doc comment (service_impl.go:15-22) declares it a deliberate lightweight repository-backed double, 'intentionally free of external dependencies... so that it can be instantiated in unit tests without any infrastructure setup,' and two of the 21 methods (ExchangeCodeForTokens, GetUserInfo) return ERR_NOT_IMPLEMENTED by design with a comment pointing at OAuthService as the production path. So the ValidateSession difference is a documented non-goal of a test double, not drift between two production implementations that were meant to agree — the finding's own framing ('have already drifted') attributes an intent the code explicitly disclaims. What survives is genuine but smaller: a security-relevant constant duplicated with no shared source and no test that would catch a one-sided edit, and a test-shaped implementation living in a production package where the repo's stated convention puts doubles in internal/testutil/mocks/. The expiry-constant leg alone justifies a ticket; the acceptance criteria remain provable as written.",
    "corrected_severity": "low",
    "ticketable": true
  },
  {
    "id": "DUP-004",
    "verdict": "confirmed",
    "evidence_reproduces": true,
    "basis": "The md5 pair reproduces (4a6b073f1c888efd002ea764f867855c twice). I read the block at DesktopRow.tsx:128-143 directly: the ten-key handlers literal, `const flags = { dhListingLoading }`, the resolveContextualPrimary call, the fallbackPrimary literal, and the resolveOverflowActions call, identical in MobileCard.tsx:55-70. The optionality claim holds — rowActions.ts:19-30 declares RowActionHandlers with onRecordSale required and the other nine optional, so a handler added to one view and forgotten in the other type-checks and the action silently vanishes from that viewport. My own `git grep -nE 'resolveContextualPrimary|resolveOverflowActions' 06d9f8ce -- web/src` confirms exactly two consumers and one definition site, so the fix surface is the two files the finding names and nothing else. Severity low is right: the resolution rules themselves are already shared in rowActions.ts, so this is duplicated wiring rather than duplicated logic.",
    "what_the_finding_got_wrong": "",
    "corrected_severity": "",
    "ticketable": true
  },
  {
    "id": "DUP-005",
    "verdict": "confirmed",
    "evidence_reproduces": true,
    "basis": "All three predicates are where the finding says. inventory.EffectiveFeePct (channel_fees.go:16-20) rejects <=0 and >=1; arbitrage/acquisition.go:41-43 restates that predicate inline; arbitrage/expected_value.go:57-60 implements only `if in.FeePct > 0`, with no upper bound, so an out-of-range fee that acquisition would replace with the default is used verbatim by EV — I read expected_value.go:50-75 and confirmed feePct flows straight into `expectedFees := int(expectedSale * feePct)` at :71 with no further clamp. The latency claim also holds: my own `git grep -nE 'computeAcquisitionOpportunity\\(|computeExpectedValue\\(|EffectiveFeePct\\(' 06d9f8ce -- internal/domain/arbitrage | grep -v _test.go` shows every production call site (service_batch.go:136, :209 via the map built at :16; service_ev.go:67, :131, :146 each preceded by an EffectiveFeePct call at :66, :130, :144) already passes a sanitized value, so neither inline guard can fire today.",
    "what_the_finding_got_wrong": "Nothing substantive. Evidence item 4's `output` field is a prose paraphrase of the command's result rather than its literal text (it summarizes service_batch.go:136 and :209 in a sentence); the command itself reproduces and the substance is accurate, but a verifier expecting verbatim output would flag the mismatch. Filed at suspected, which is the correct tier for a divergence that no current caller can reach.",
    "corrected_severity": "",
    "ticketable": false
  },
  {
    "id": "ARCH-001",
    "verdict": "confirmed_lower_severity",
    "evidence_reproduces": true,
    "basis": "A fail-open claim is testable by construction, so I built fixtures in /tmp (outside the repo, nothing written under it) from `git show 06d9f8ce:scripts/check-imports.sh` and ran the baseline checker against each. Fixture A (internal/domain/ present, sib1 importing internal/adapters/clients/dh): correctly flagged, exit 1 — pass 1 works when its path exists. Fixture C (internal/adapters/storage/postgres importing a client): correctly flagged, exit 1. Fixture D — same real storage-to-clients violation, with internal/adapters/storage renamed to internal/adapters/persistence: the checker printed 'Architecture check passed: no domain -> adapter or storage -> clients imports.' AND 'Flat sibling rule check passed' and exited 0. That is an end-to-end fail-open on a live violation, and it settles the central claim. I also confirmed the second leg by reading scripts/check-imports-test.sh: its only mkdir is `mkdir -p \"$root/internal/domain/$pkg\"` at line 29, so no fixture ever creates internal/adapters/ and pass 2 evaluates nothing in all five cases, while pass 1 is never given an adapter-importing file. The self-test genuinely has zero coverage for either hexagonal check. The `2>/dev/null || true` construction at lines 30 and 42 is as quoted, and the contrast with the flat-sibling section's four fail-closed guards is real.",
    "what_the_finding_got_wrong": "Evidence item 2 overstates the blast radius, and it is the claim that carries 'high'. It asserts that any rename 'turns both checks into vacuous passes that still print Architecture check passed' — the print happens, but the script does not pass. Fixture B (internal/domain renamed to internal/domainv2, violating file intact) printed the false pass line and then exited 1, because `domain_files=$(find internal/domain ...)` comes back empty and the downstream 'found no non-test .go files' guard fires. Fixture E (module path rewritten from guarzo/slabledger to acme/newname throughout) likewise printed the false pass and then exited 1 via the 'derived 0 sub-package(s)' guard. So for pass 1 and for the module-rename variant, the fail-open is masked at script level and `make check` still fails; only the storage-to-clients check is fail-open all the way to exit 0, because nothing downstream touches internal/adapters/. Combined with the invariant being satisfied at this baseline (which the finding correctly states), the realistic consequence is one vacuous check plus a self-test that would not notice either check breaking — a real gap in the project's sole hexagonal gate, but medium, not high. The proposed fix and all four acceptance criteria remain correct and provable as written; the renamed-directory criterion should target internal/adapters/storage, where I demonstrated the exit-0 pass, rather than only internal/domain.",
    "corrected_severity": "medium",
    "ticketable": true
  },
  {
    "id": "ARCH-002",
    "verdict": "confirmed_lower_severity",
    "evidence_reproduces": true,
    "basis": "Both edges reproduce exactly under my own pinned greps. Upward: seven non-test files under internal/platform/ import internal/domain packages — cardutil/normalize.go:19 and normalize_cards.go:6 (domain/constants), cardutil/trace.go:6 and telemetry/slog_logger.go:8 (domain/observability), config/validation.go:9 and resilience/retry.go:12 (domain/errors), resilience/retry.go:14 (domain/observability). Downward: internal/domain/{csvimport,inventory,liquidation} import platform/cardutil and domain/psacampaign imports platform/canonjson. internal/README.md:63-66 states the rule as inward-only and enumerates adapters->domain, domain->platform, and domain->adapters (never); no platform->domain edge is sanctioned anywhere in that block. `git show 06d9f8ce:scripts/check-imports.sh | grep -n 'internal/platform'` returns only lines 6 and 51, both prose inside a comment and an echo — I read the whole script and confirmed no pass scans internal/platform in either direction. The stale-taxonomy observation also holds: README lists errors/ under internal/platform (lines 56, 243) while the package lives at internal/domain/errors.",
    "what_the_finding_got_wrong": "The title's 'depend on each other' is technically true of the two directories but reads as a structural coupling problem that does not exist. Every one of the seven upward edges targets domain/constants, domain/errors, or domain/observability — three dependency-free leaf packages, in the precise sense internal/README.md defines (transitive closure excludes the inventory hub). No import cycle exists, and Go would reject one if it did, so nothing here impedes changing either layer. What is actually wrong is that the documented arrow does not describe the code and no check would notice if a platform package started importing something heavier than a leaf. That is a documentation-and-enforcement gap rather than a medium-severity architecture defect, and the finding's own proposed fix (option (a): sanction the three-package allowlist in the Dependency Rule and add a check) is the right shape for it. Acceptance criteria are provable as written.",
    "corrected_severity": "low",
    "ticketable": true
  },
  {
    "id": "ARCH-003",
    "verdict": "confirmed",
    "evidence_reproduces": true,
    "basis": "Re-derived from source. internal/adapters/httpserver/handlers/cardladder.go:11 imports internal/adapters/storage/postgres; :31 declares `store *postgres.CardLadderStore`; :47 takes the concrete type in NewCardLadderHandler. The same struct holds three consumer-defined interfaces (refresher CLRefresher declared at :16, purchaseLister CLPurchaseLister, syncUpdater CLSyncUpdater), so the concrete dependency is an outlier inside one struct rather than a package convention. My own `git grep -nE '\"github\\.com/guarzo/slabledger/internal/adapters/storage' 06d9f8ce -- 'internal/adapters/httpserver/*'` returns exactly one line — cardladder.go:11 — establishing this as the only inbound-adapter-to-persistence import in the whole httpserver tree, and that is an absence claim resting on a single-line output rather than a citation. pricing_api.go:14-19 carries the opposite pattern with the 'Defined at the consumer level per idiomatic Go / Satisfied by *postgres.PurchaseStore' comment, so the house style is documented in place. The seam is seven methods (GetCLCoverageByMonth, GetCLFailures, GetCLPriceStats, GetConfig, ListMappings, SaveConfig, SaveMapping), reproduced from the finding's command, so the extraction is bounded and the effort estimate is credible. Medium is right: no runtime defect, but it blocks mock-based handler tests and is the single obstacle to an httpserver-scoped enforcement rule.",
    "what_the_finding_got_wrong": "",
    "corrected_severity": "",
    "ticketable": true
  },
  {
    "id": "ARCH-004",
    "verdict": "confirmed",
    "evidence_reproduces": true,
    "basis": "Judged as what it is — a pointer handing a docs-category lead to the docs lens — so the test is whether the lead is sound and specific enough to act on, not whether it stands as a full finding. All three legs reproduce under my own commands. internal/README.md names cache/ (lines 52, 240) and errors/ (lines 56, 243) under internal/platform, and `git ls-tree -r --name-only 06d9f8ce -- internal/platform | xargs -n1 dirname | sort -u` returns only canonjson, cardutil, config, crypto, resilience, storage, telemetry — neither exists. README:138 cites `advisor -> ai` and `advisor -> scoring` as worked examples of legal edges, and `git ls-tree -r --name-only 06d9f8ce -- internal/domain/advisor internal/domain/ai internal/domain/scoring` returns empty, consistent with commit e46370e5. The derived governed-sibling set is eleven — arbitrage, csvimport, demand, dhlisting, dhpricing, export, finance, portfolio, pricing/lookup, psacampaign, tuning — against the ten CLAUDE.md enumerates; csvimport is the omission, and since the checker derives membership from the tree, csvimport is genuinely governed while going unlisted. low/suspected is the correct filing and the pointer stays in its lane.",
    "what_the_finding_got_wrong": "Worth flagging for whoever actions it, though it does not weaken the lead: CLAUDE.md's sibling sentence is prefixed with 'derive it rather than trusting this sentence' and the surrounding section tells the reader to compute the set, so the ten-vs-eleven gap is a self-disclaimed staleness rather than a doc asserting something false. The README items are unqualified and are the substantive half.",
    "corrected_severity": "",
    "ticketable": false
  }
]
