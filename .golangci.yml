version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - misspell
    - revive
  settings:
    gocritic:
      enabled-tags:
        - diagnostic
        - style
      disabled-checks:
        - paramTypeCombine
        - whyNoLint
        - unnamedResult
        - httpNoBody
        - octalLiteral
        - ifElseChain
        - elseif
        - emptyStringTest
        - returnAfterHttpError
        - commentedOutCode
    revive:
      rules:
        - name: exported
          disabled: true
        - name: unexported-return
          disabled: true
  exclusions:
    paths:
      - web/node_modules
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gocritic
      - path: testutil/mocks/
        linters:
          - unused
          - revive

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
