---
description: "Create a new database migration pair (up + down SQL files)"
argument-hint: "[description of the schema change]"
---

Use the `skill` tool to load the `new-migration` skill, then execute its full workflow.

The argument passed to this command is: $ARGUMENTS

Pass that argument to the skill as the description of what schema change is needed.
