---
description: "Holistic codebase review — quality, maintainability, dead code, duplication, UX friction — top-10 improvements"
argument-hint: "[backend | frontend | <package>]"
---

Use the `skill` tool to load the `improve` skill, then execute its full workflow.

The argument passed to this command is: $ARGUMENTS

Pass that argument to the skill as the scope (empty means full codebase, `backend` for Go only, `frontend` for React only, or a package name like `campaigns` or `dhprice` for a deep dive).
