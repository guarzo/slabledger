---
description: "Add a new HTTP API endpoint following the hexagonal architecture pattern"
argument-hint: "[description of the endpoint]"
---

Use the `skill` tool to load the `new-handler` skill, then execute its full workflow.

The argument passed to this command is: $ARGUMENTS

Pass that argument to the skill as the description of what endpoint to create.
