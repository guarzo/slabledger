// Shared column-width constants for the desktop inventory table. The header
// (InventoryTab.tsx) and row cells (DesktopRow.tsx) live in different files
// but must use the same widths or the columns visually drift apart.

export const ACTIONS_COLUMN_WIDTH = '144px';
