/** Small-caps section label shared by the admin tab shells. Sections that lead
    a tab use a larger, higher-contrast heading instead — see PricingTab. */
export const SECTION_HEADER =
  'text-sm font-semibold uppercase tracking-wider text-[var(--text-muted)] mb-3';
